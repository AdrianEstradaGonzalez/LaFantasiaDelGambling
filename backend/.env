PORT=3000
JWT_SECRET=super_secret_key
# Railway → Settings → Variables → copia la cadena completa:
# Usa la URL del apartado “PostgreSQL Connection URL” y añade sslmode=require.
# Si usas el Pool de Railway, usa la del pool con pgbouncer.
DATABASE_URL="postgresql://postgres:qNQRVFyGnbjfARnIvWyKBwrAFoTdeqFq@mainline.proxy.rlwy.net:56424/railway"

APP_ORIGIN="https://tu-frontend.com"   
EMAIL_FROM="Soporte Fantasy <soporte@tu-dominio.com>"
SMTP_HOST="smtp.tu-proveedor.com"
SMTP_PORT=587
SMTP_USER="soporte@tu-dominio.com"
SMTP_PASS="TU_PASSWORD_SMTP"
SMTP_SECURE=false       # true si usas 465

# Config reset
RESET_CODE_TTL_MINUTES=30
RESET_CODE_MAX_ATTEMPTS=5
